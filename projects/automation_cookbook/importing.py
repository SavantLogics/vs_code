# This gets the x module; you may access its functions by accessing the module
import math

print(math.factorial(9))

# NOTE- Using this method is inadvisable, because imported definitions may conflict with other definitions. It is better to import only the ones that you need to complete your project 